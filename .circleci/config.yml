version: 2.1

jobs:
  doomed-job:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Intentionally fail"
          command: |
            echo "Summoning the Great CI Gremlin 🐉"
            exit 1   # <- This guarantees failure

workflows:
  doomed-workflow:
    jobs:
      - doomed-job













