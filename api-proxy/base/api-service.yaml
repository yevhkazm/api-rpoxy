---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: api-proxy
  name: api-proxy
  namespace: api-proxy
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: api-proxy
